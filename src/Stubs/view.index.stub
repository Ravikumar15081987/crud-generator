@extends('{{ layoutName }}')

@section('title', 'Manage Index - {{ modelName }}')

@section('content')
    <div class="container-fluid py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="mb-0">{{ModelName}} Management</h3>
            <a href="{{ route('{{role}}.{{model-plural-kebab}}.create') }}" class="btn btn-primary">
                <i class="ri-add-line me-1"></i> Add {{ModelName}}
            </a>
        </div>

        <!-- Livewire Filters Component -->
        <livewire:data-table-filters 
            datatableId="{{ModelName}}TableId" 
            :config="$filtersConfig" 
        />

        <!-- Livewire DataTable Component -->
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <livewire:data-table 
                    model="{{ModelName}}" 
                    id="{{ModelName}}TableId" 
                    :configPath="'datatable.{{role}}.{{model-plural-kebab}}'" 
                />
            </div>
        </div>
    </div>
@endsection