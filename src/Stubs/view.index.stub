@extends('{{ layoutName }}')

@section('title', 'Manage {{ modelName }}s')

@section('content')
    <div class="container-fluid py-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h3 class="mb-0">{{ modelName }} Management</h3>
            <a href="{{ route('{{ routeName }}.create') }}" class="btn btn-primary">
                <i class="ri-add-line me-1"></i> Add {{ modelName }}
            </a>
        </div>
        
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h4 class="mb-0">{{ $modelName }} List</h4>
            
            <div class="d-flex gap-2">
                <!-- View Trashed Records Button -->
                <a href="{{ route($routeName . '.trashed') }}" class="btn btn-outline-danger btn-sm d-flex align-items-center gap-1">
                    <i class="bx bx-trash"></i> View Trash
                </a>

                <!-- Create New Record Button -->
                <a href="{{ route($routeName . '.create') }}" class="btn btn-primary btn-sm d-flex align-items-center gap-1">
                    <i class="bx bx-plus"></i> Add {{ $modelName }}
                </a>
            </div>
        </div>

        <!-- Livewire Filters Component -->
        <livewire:data-table-filters 
            datatableId="{{ modelName }}TableId" 
            :config="$filtersConfig ?? []" 
        />

        <!-- Livewire DataTable Component -->
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <livewire:data-table 
                    model="{{ modelName }}" 
                    id="{{ modelName }}TableId" 
                    :configPath="'datatable.{{ role }}.{{ model-plural-kebab }}'" 
                />
            </div>
        </div>
    </div>
@endsection