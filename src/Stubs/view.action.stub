<div class="flex items-center space-x-2">
    <a href="{{ route('{{ routeName }}.show', $row->id) }}" class="text-blue-600 hover:text-blue-900">View</a>
    <a href="{{ route('{{ routeName }}.edit', $row->id) }}" class="text-indigo-600 hover:text-indigo-900">Edit</a>
    <form action="{{ route('{{ routeName }}.destroy', $row->id) }}" method="POST" class="inline">
        @csrf
        @method('DELETE')
        <button type="submit" class="text-red-600 hover:text-red-900" onclick="return confirm('Are you sure?')">Delete</button>
    </form>
</div>