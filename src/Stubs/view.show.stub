@extends('{{ layoutName }}')

@section('title', 'Show {{ modelName }}')

@section('content')
    <div class="container-fluid py-4">
        <h4 class="fw-bold text-dark mb-4">{{ modelName }} Details</h4>
        
        <div class="card border-0 shadow-sm rounded-4 p-4">
            <!-- Detailed view content goes here -->
            <p class="text-muted">Details for this {{ modelName }} will be displayed here.</p>
        </div>

        <div class="mt-4">
            <a href="{{ route('{{ routeName }}.index') }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
@endsection